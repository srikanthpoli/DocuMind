# DocuMind Frontend

Angular web application.

## Run

From this `frontend` folder:

```powershell
npm install
npm start
```

Open the application at http://localhost:4200.

The backend must be running separately at http://127.0.0.1:8000.
